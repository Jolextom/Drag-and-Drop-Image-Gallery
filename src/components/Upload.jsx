import React from "react";

const Upload = () => {
  return (
    <div className="absolute border bottom-0 w-full h-10 flex items-center">
      <input type="file" name="image upload" />
    </div>
  );
};

export default Upload;
